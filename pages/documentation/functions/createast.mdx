# createAst()

`createAst(){:js}` is the function that creates the *AST* of the parser.

```js copy
createAst(
	`{newEmbed:
        {title:hi}
        {description:hello}
        {color:Random}
        {timestamp}
        {footer:hi}
    }`
);
```
